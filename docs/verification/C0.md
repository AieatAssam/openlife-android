# Capability 0 verification record

This file is updated as each stage in the implementation plan lands. Every row
must eventually carry a real command, a real result, and a build/device
identifier — a row is never marked passed without having actually been run.
"Not run" is a legitimate, honest status; "assumed" is not a status this file
uses.

Status legend: **not started** (no code yet) · **not run** (code exists,
verification pending) · **pass** · **fail** · **gap** (cannot be run in this
environment; see note).

| ID | Test | Status | Evidence / notes |
| --- | --- | --- | --- |
| C0-01 | Manifest/dependency inventory: no INTERNET, broad access, telemetry, exported vault reader | pass | **Re-ran** 2026-09-14 with the full app in place: `./gradlew :app:processDebugMainManifest :app:processReleaseMainManifest`, inspected at `app/build/intermediates/merged_manifest/{debug,release}/.../AndroidManifest.xml`. Neither declares INTERNET or any broad-access permission (the only `<permission>`/`<uses-permission>` pair is AndroidX's own `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`, signature-protected, unrelated to network/storage access). Release manifest exports exactly two activities: the real `MainActivity` (source list, `LAUNCHER`, exported because Android requires the launcher activity to be) and `IntakeActivity` (the one intentional `ACTION_SEND` intake surface, design §8/§11) — no other component (no provider, service, or receiver reading vault data) is exported. Standard AndroidX components present: `androidx.startup.InitializationProvider` (unexported), `androidx.room.MultiInstanceInvalidationService` (unexported), `androidx.profileinstaller.ProfileInstallReceiver` (exported but `DUMP`-permission-protected, a stock AndroidX default). Debug manifest adds three things, confirmed absent from release: `TestHostileContentProvider` (`net.openlifetest.hostileprovider`, **unexported**, debug-only per design §13), and two standard Compose-tooling activities (`PreviewActivity`, `ComponentActivity`) pulled in by `debugImplementation("androidx.compose.ui:ui-tooling")`. Dependency inventory: `./gradlew :app:dependencies --configuration releaseRuntimeClasspath` — every dependency is AndroidX/Jetpack Compose, Kotlin stdlib/coroutines/serialization-core, Room/SQLite, or `net.zetetic:sqlcipher-android`; no analytics, crash-reporting, ads, or networking library anywhere in the release runtime classpath. `androidx.window` appears only as a transitive dependency of `androidx.activity`/Compose (foldable-device support), unused by any OpenLife code and not network-capable. |
| C0-02 | Import synthetic JPEG/PNG by share and picker; digest equality; READY only after confirmation | partial | **Ran** 2026-09-13 on `dev36` (API 36): `./gradlew :vault:connectedDebugAndroidTest` — `ImportRepositoryTest` 10/10 pass, including digest equality and byte-for-byte round trip through the real encrypted stage file at prepare time, and (Stage 4) `saveTransitionsStagedToReadyAndRenamesStageToBlob`/`savedBlobAuthenticatesAndReproducesTheExactOriginalBytes` confirming the row only becomes READY after an explicit Save. Stage 6 adds the real Share route end-to-end: `IntakeActivityTest.validShareImportsSuccessfully` drives `IntakeActivity` with a real `ACTION_SEND` intent and a real `content://` provider (`TestHostileContentProvider`, debug-only) through to `PrepareResult.Prepared`. Stage 7 adds the Photo Picker route (`MainActivity` forwards a picked URI to `IntakeActivity` as a same-app `ACTION_SEND` intent carrying `EXTRA_INTAKE_KIND`, sharing one validated pipeline) and confirms the full round trip through the real UI layer: `IntakeAndListFlowTest.savingThroughIntakeViewModelMakesTheSourceAppearInTheListAndDeletionRemovesIt` drives `IntakeViewModel.startImport` → `confirmSave` and confirms the saved Source appears in `SourceListViewModel`'s live query, then deletes it. Manually confirmed rendering end-to-end via `uiautomator dump` on `dev36` (screenshots are black by design — `FLAG_SECURE` blocks `adb screencap`, confirmed via `dumpsys window` showing the `SECURE` flag applied): first-run explanation text and button, then the list's app bar, "Import from photos" action, and empty-state message. |
| C0-03 | Change provider bytes after preview | partial | **Ran** 2026-09-13 on `dev36`: `IntakeActivityTest.changedBytesAfterOpenAreNotSilentlySubstituted` — the test provider serves the real bytes on the first (only) read and would serve different bytes on any second read; the import still succeeds, proving `prepareImport` reads the source exactly once and never re-reads the provider afterward. |
| C0-04 | Cancel picker/preview, rotate, background, kill process | pass | **Ran** 2026-09-13 on `dev36`: `IntakeAndListFlowTest.cancellingAPreviewDiscardsItWithoutSaving` confirms `IntakeViewModel.cancel()` removes the STAGED row/file and it never appears in the list. `.restoringFromASavedStateHandleReAuthenticatesTheStagePreview` simulates a rotation/process-recreation by constructing a fresh `IntakeViewModel` over a `SavedStateHandle` that already holds only the Source UUID (never plaintext bytes, per design §8) and confirms it re-authenticates the stage from disk and reproduces the same preview metadata. **Real backgrounding + kill ran** 2026-09-14 (see C0-08 for the shared reboot evidence): a saved Source's SHA-256, read back through the real `SourceViewRepository.loadReadyBytes` path, was logged, the app was sent to the background then killed with a real `kill -9`, and relaunching reproduced the identical digest with no crash. |
| C0-05 | Multiple/conflicting URIs, file/http URI, own authority, missing/expired grant | partial | **Ran** 2026-09-13 on `dev36`: `IntakeActivityTest` drives `IntakeActivity`'s exported component directly (bypassing intent-filter resolution, per design §8's "validate every incoming intent regardless of the filter") with: multiple `ClipData` items, a `file://` URI, and a wrong action — all rejected before touching any provider. Unit-level (JVM, `./gradlew :app:test`): `IntakeIntentValidatorTest` (13 tests) additionally covers conflicting `data`/`EXTRA_STREAM`, an `http://` URI, exact and nested own-authority matches, a look-alike authority for a different app, and a malformed URI string. Missing/expired grant is covered by `unavailableProviderIsRejectedGracefully`, which found a real bug (below). |
| C0-06 | Forged MIME, corrupt headers, animation, extreme dimensions, excess bytes | partial | Unit-level (JVM, `./gradlew :vault:test`): `ImageHeaderValidatorTest` (16 tests, pass) covers forged MIME both directions, unrecognised magic bytes, truncated/malformed PNG and JPEG headers, animated PNG (APNG `acTL` before `IDAT`), longest-edge and pixel-count limits (including a near-4-billion overflow-adjacent case), zero dimension, and the byte-count boundary. `BoundedStreamReaderTest` (4 tests, pass) covers the byte-limit boundary and digest correctness. End-to-end rejection through `ImportRepository` **ran** 2026-09-13 on `dev36`: `ImportRepositoryTest.rejectedImportLeavesNoStagedRowOrFile` and `.forgedMimeTypeMismatchIsRejectedAndCleanedUp` pass — no STAGED row or stage file survives a rejection. Multiple/conflicting-URI and animation-via-real-decoder cases remain for Stage 6/8. |
| C0-07 | Exact duplicate import | partial | **Ran** 2026-09-13 on `dev36`: `ImportRepositoryTest.savingAnExactDuplicateDiscardsTheNewStageAndPreservesTheExisting` — saving a second import with identical bytes returns `SaveResult.DuplicateFound(existingId)`, the original READY row/blob are untouched, and the new STAGED row/stage file are fully removed (no half-saved second copy). The reboot-persistence leg of "existing original preserved" is covered by C0-08. |
| C0-08 | Restart and reboot after successful save | pass | **Ran** 2026-09-13 on `dev36`: `VaultBootstrapperTest.restartReusesTheSameSecretAndPersistedDataSurvives` (Stage 2) proves the database secret and a persisted row both survive a simulated restart (fresh wrapper/database objects over the same files). **Real reboot leg ran** 2026-09-14: saved one Source through the real UI (share intent → preview → Save), logged its SHA-256 via a one-off instrumented harness reading through the real `SourceViewRepository.loadReadyBytes` path, then ran a genuine `adb reboot` and waited for `sys.boot_completed=1`. After reboot, the same harness reproduced the byte-identical digest, and the real list UI (`uiautomator dump`) showed the item with its original import timestamp ("Imported Sep 14, 2026 12:53 AM") — no crash, no CORRUPT state, no data loss across a real device reboot. |
| C0-09 | Kill process at every persistence checkpoint | pass | **Ran** 2026-09-13 on `dev36`: `RecoveryRepositoryTest` (8/8) simulates a kill at every checkpoint by directly constructing the interrupted on-disk state recovery must resolve. **True process-kill leg ran** 2026-09-14, host-driven since a process can't observe its own `kill -9` from inside the same test JVM: `TestHostileContentProvider` gained a `LARGE_FIXTURE_NAME` URI that generates an ~11.6 MB real JPEG on demand (same-app, same-uid, so no cross-app URI-grant issue — an earlier attempt using a `content://media/...` URI inserted by the test process failed with `SecurityException: org.openlife has no access`, since `adb shell am start`'s shell identity can't grant access to a MediaStore row it doesn't own). Four separate real `adb shell kill -9 <pid>` kills, each timed via `am force-stop` + fresh `am start` + a tuned `sleep` (3.65s–3.95s, the actual bootstrap-to-Prepared window on this cold, loaded emulator) landed **while `prepareImport` was actively writing** — confirmed by a distinct, freshly-created `<uuid>.stage` file present at each kill. Relaunching after every kill logged `RecoveryReport(cleanedStaged=1, ...)` and left exactly one (the new) stage file behind each time — no accumulation, no `FATAL EXCEPTION` in logcat, no CORRUPT row surfaced for what was always safely-discardable STAGED work. Kill-during-Save was also attempted (tapping "Save" via `adb shell input tap` then killing 30–150 ms later): the rename+fsync+DB-commit completed within the shell round-trip's own latency every time (same resulting `.blob` UUID and unchanged MD5 across all four attempts, duplicate-detected on retries since the fixture's content is deterministic) — recorded honestly as a **negative result**: this environment's `adb`-level control resolution (tens of ms per round trip) cannot externally interrupt a `rename()` step that completes faster than that, and POSIX `rename()` is atomic by construction (no partial-file state exists to hit). The STAGED-with-orphan-blob and mid-transaction-DB-failure interleavings that a slower rename *could* expose remain covered deterministically by `RecoveryRepositoryTest`'s constructed states, which stay the only practical way to force those exact interleavings on demand. |
| C0-10 | Fill storage; inject read/write/sync/rename/commit failures | partial | **Ran** 2026-09-13 on `dev36`: `ImportRepositoryTest.savingATamperedStageFailsAndLeavesTheRowStaged` covers a corrupted-stage Save attempt returning `Failed` with the row left STAGED, not silently reported as saved. The true-process-kill runs described under C0-09 additionally exercise real (not simulated) write interruption during `prepareImport`, with clean self-healing recovery every time. A genuine full-storage condition and a rename forced to fail at the filesystem level still need a dedicated fault-injection seam and remain a gap. |
| C0-11 | Modify ciphertext, tag, nonce, bound UUID, envelope length | pass | Stage 1 (unit-level, JVM, `./gradlew :vault:test`): `AesGcmCodecTest` (13 tests) covers wrong key, tampered ciphertext byte, tampered tag byte, tampered nonce byte, wrong domain label, wrong bound Source UUID, domain-only AAD rejecting a source-bound ciphertext and vice versa, and a ciphertext truncated below the tag length — every case throws `AesGcmCodec.AuthenticationFailedException`, never returns plaintext. `EnvelopeCodecTest` (11 tests) covers bad magic, unsupported version, wrong declared nonce length, negative/oversized/mismatched declared ciphertext length, and truncated headers, all before any authentication step runs. `GoldenEnvelopeFixtureTest` (2 tests) pins the exact on-disk byte layout against a committed fixture (`vault/src/test/resources/envelope/golden-artefact-v1.hex`). **End-to-end leg ran** 2026-09-14 on `dev36`: `EnvelopeTamperingThroughRepositoryTest` (3/3, `:vault:connectedDebugAndroidTest`) saves a real `Source` through `ImportRepository.saveImport` (real Keystore, real SQLCipher row, real `.blob` file under `noBackupFilesDir`), then flips one byte in the actual on-disk ciphertext and separately flips one byte in the actual stored wrapped-DEK column — both come back `null` from `SourceViewRepository.loadReadyBytes`, the same method the real viewer UI calls, with a control test confirming the untampered path still returns plaintext (so the negative results aren't from a broken setup). |
| C0-12 | Remove/invalidate key; fail wrapper or database open | pass | **Ran** 2026-09-13 on `dev36` (API 36, `emulator -avd dev36 -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect -no-snapshot -no-metrics -crash-report-mode disabled -cores 1`): `./gradlew :vault:connectedDebugAndroidTest` — 20/20 instrumented tests pass. `VaultBootstrapperTest` (5/5): fresh bootstrap, restart reusing the same secret, an interrupted-bootstrap retry, a database-without-a-usable-key-file state (`Unavailable`, nothing deleted/replaced), and a corrupt key file (same). `KeystoreWrapperTest` (6/6): key non-exportability, wrap/unwrap round trip, fresh IV per wrap, wrong-bound-UUID and tampered-ciphertext failing closed. `OpenLifeDatabaseReadyInvariantTest` (4/4): the SQL-trigger enforcement of the READY invariant (`docs/decisions/0001-c0-defaults.md` item 8) rejects both an incomplete insert and a premature STAGED→READY update. Getting here surfaced and fixed two real bugs first: (1) two `@Test fun x() = runBlocking { ... }` methods across `ImportRepositoryTest`/`OpenLifeDatabaseReadyInvariantTest`/`VaultBootstrapperTest` had non-`Unit` inferred return types (JUnit requires `void`), fixed by adding explicit `: Unit`; (2) `net.zetetic:sqlcipher-android`'s native library was never loaded (`UnsatisfiedLinkError` on `nativeOpen`) — an earlier static-analysis conclusion that it self-loaded was wrong; `OpenLifeDatabaseFactory` now calls `System.loadLibrary("sqlcipher")` before every open (see `docs/decisions/0001-c0-defaults.md` item 3's correction). |
| C0-13 | Inspect files, database, WAL, temp files, backup/transfer output | pass | **Ran** 2026-09-14 on `dev36` (`adb shell run-as org.openlife find/stat/cat` after saving one real Source): every file under `noBackupFilesDir/vault` is mode `600`, every directory `700` (except the OS-owned `no_backup` parent itself, `771`, standard Android sandboxing). Pulled and inspected byte-for-byte with `od`/`strings`: the `.blob` artefact (12,184,701 bytes) starts with only the `OLV1` envelope magic, contains **zero** occurrences of the JPEG SOI+APP0 marker (`FFD8FFE0`) anywhere in the file, and no `JFIF`/`EXIF` ASCII string anywhere — fully opaque ciphertext, not "encrypted-looking but somehow recoverable." `openlife.db` doesn't even show the plaintext `SQLite format 3` header (SQLCipher encrypts at the page level); `openlife.db-wal` was searched the same way for the sources table's own content and schema keywords (`CREATE TABLE`, `sources`) — no matches. `database.key` is itself a wrapped envelope (`OLV1` magic), never a raw AES key on disk. `cache/`/`code_cache/` were also inspected and initially turned up a real (if minor) finding: **the debug-only `TestHostileContentProvider` left a `hostileNNNN.tmp` file with the served test bytes sitting in `cacheDir` indefinitely** — found by this on-device inspection, not by code review. Confirmed via `grep -rn cacheDir` that no production code (`app/src/main`, `vault/src/main`) touches `cacheDir` or `createTempFile` at all, so this was purely test-fixture hygiene, not a shipped defect; fixed by deleting the temp file immediately after opening it (the open fd keeps it readable until closed, so nothing observable changes for the tests that rely on it), and re-confirmed empty `cache`/`code_cache` after the fix. `backup_rules.xml`/`data_extraction_rules.xml` (inspected as packaged, C0-01's manifest check already confirms `allowBackup=false` is actually applied) exclude every domain (`root`, `file`, `database`, `sharedpref`, `external`) from both legacy full-backup and Android 12+ cloud-backup/device-transfer; the vault itself lives under `noBackupFilesDir`, already excluded by definition, with these exclusions stated explicitly as defence-in-depth. Actually triggering a device-to-device transfer or Auto Backup pass end-to-end is not exercisable on this single emulator and remains a documented **gap** (see Known environment limitations). |
| C0-14 | Delete while viewing; fail file deletion; kill during deletion | partial | **Ran** 2026-09-13 on `dev36`: `DeletionRepositoryTest` (6/6) — deleting a READY or CORRUPT Source removes its row and blob; an unknown or STAGED source ID returns `NotFound` (deletion is not the STAGED-cancellation path); retrying an already-`DELETING` row (simulating a kill after files were removed but before the row was) finishes the job; and a file that cannot be removed (a locked directory standing in for a real deletion failure) leaves the row `DELETING` and returns `Failed`, never a false "deleted". Stage 7's `ViewerScreen`/delete confirmation dialog wire this up in the real UI (delete confirmation names the item visually, per design §8), but no view-lease mechanism exists yet to test "delete while viewing" racing plaintext delivery specifically — deferred to Stage 8. |
| C0-15 | Inspect logs, recents, screenshots, notifications, outbound intents | pass | **Ran** 2026-09-14 on `dev36`: exercised a full real lifecycle (share → preview → Save → list → viewer → delete confirmation → deleted) while capturing `adb logcat -d` end to end, then filtered for every OpenLife-related line. Every line naming a file, URI, or byte count was from the *system* (`ActivityManager`, `MediaProvider`, `WindowManager`, IME service, none of it app-emitted); the only line the app itself ever logs is the already-reviewed `OpenLifeRecovery: RecoveryReport(cleanedStaged=..., confirmedReady=..., ...)` — counts only. Confirmed by direct code inspection this is exhaustive: `grep -rn "Log\.\|println" app/src/main vault/src/main` returns exactly that one call site, nothing else in either module's shipping code logs anything. Recents: `adb shell dumpsys window org.openlife` shows `fl=... SECURE ...` on every OpenLife window (the same `FLAG_SECURE` already confirmed in Stage 7 to blank `adb screencap`) — `FLAG_SECURE` suppresses the recents-switcher thumbnail through the identical OS mechanism, and `dumpsys activity recents` shows no custom `taskDescription` bitmap for the task either. Clipboard: `grep -rn "ClipboardManager\|setPrimaryClip"` across both modules returns nothing — the app never touches the clipboard. Outbound intents: `grep -rn "startActivity\|ACTION_SEND"` across `app/src/main` finds exactly one `startActivity` call, in `MainActivity` forwarding a Photo Picker result to `IntakeActivity` as a same-app, explicit-component intent (Stage 6/7's own documented design) — there is no code path anywhere that sends a `Uri`, filename, or byte content to another app or component. Notifications: the app declares no notification channel and never calls `NotificationManager`, confirmed absent from both the manifest and a `grep -rn Notification` across `app/src/main`. |
| C0-16 | Lowest supported API, current target API, TalkBack, large text | partial | Current-target-API (37) leg: every instrumented and manual UI check in this file already runs on `dev36` (API 36 system image, `targetSdkVersion 37`), so this leg is covered throughout, not as a separate pass. **Large-text leg ran** 2026-09-14: `adb shell settings put system font_scale 1.3` then `2.0` (Android's own largest accessibility step), then walked every screen (first-run, empty list, preview/save, list-with-item, viewer, details, delete confirmation) via `uiautomator dump`, checking every node's `bounds` against the 1080×2400 screen — nothing clipped or exceeded the screen at either scale (worst case, the delete confirmation dialog's body text, still ended at y=1639 of 2400 at 2.0×). Icon-only controls audit (`grep -rn "Icon("` across `app/src/main/java/.../ui`): every `Icon(...)` call site (Back, Delete ×2, Add, Warning) carries a non-empty `contentDescription`, confirmed rendered by `uiautomator` showing `content-desc="Back"` / `content-desc="Delete"` on those nodes. **TalkBack leg remains a gap**: the content-description audit above and the fact every visible label is a real `Text`/`Icon contentDescription` (not a canvas-drawn glyph) gives reasonable confidence, but this environment cannot run TalkBack itself and confirm actual spoken/focus-order behavior (no accessibility-service automation was set up for this pass) — recorded honestly as unverified, not passed. **Lowest-API (29) leg remains a known gap** — see `docs/decisions/0001-c0-defaults.md` item 4; no API 29 system image can be installed in this environment. |
| C0-17 | Slow/blocking provider, repeated list scrolling at limits | pass | **Ran** 2026-09-14 on `dev36` and found a real bug: `TestHostileContentProvider` gained a bounded `artificialDelayMillis`/`"slow-<ms>.jpg"` delay (a genuine `Thread.sleep`, always returns on its own — not the earlier unwritten-pipe mechanism that caused the Stage 7/8 fd-hazard hang). A 6-second delay reproduced a **real Android ANR** (`logcat`: `ActivityManager: ANR in org.openlife ... Reason: Input dispatching timed out ... Waited 5000ms for FocusEvent`) — `IntakeActivity.startImportFromUri` called `contentResolver.openInputStream(uri)` directly on the Compose/main thread with no timeout of its own; the existing 15-second cooperative-cancellation deadline in `IntakeViewModel.startImport` only ever covered *reading* an already-opened stream, never the *open* call itself. **Fixed** by moving `getType`/`openInputStream` onto `Dispatchers.IO` via `lifecycleScope.launch` in `IntakeActivity.startImportFromUri` — re-tested with the same 6s delay: tap-to-main-thread round trip stayed at 56–130ms throughout (vs. the prior freeze), the UI correctly showed a live "Preparing…" the whole time, no ANR in logcat, and the import still completed once the provider responded. Locked in as `IntakeActivityTest.slowProviderOpenStaysResponsiveInsteadOfFreezingTheActivity`, which uses the real activity lifecycle's own `onActivity` polling as an implicit responsiveness check — a regression back to a frozen main thread would show as this test timing out, not just running slow. List-at-volume leg: `IntakeAndListFlowTest.listHoldsManySourcesWithoutLosingOrMisorderingAny` saves 15 real, distinct sources (each with genuinely different pixel content — an initial version of this test reused byte-identical content across iterations and inadvertently exercised C0-07's duplicate-detection path instead, discovered via a live thread dump when it hung; fixed by varying the bitmap color per iteration) and confirms the real `SourceListViewModel`/Room-Flow-backed list holds all 15 with none dropped or duplicated. A manual on-device swipe over a populated list produced no crash or ANR, though environment flakiness (`pm clear` behaving like a full uninstall at least twice this session — see Known environment limitations) meant the swipe itself landed on a smaller list than intended; the automated 15-item test is the stronger evidence here. |

## Build identity

Filled in at Stage 8 (or updated per stage where relevant): commit hash,
`applicationId`/`versionCode`, Gradle/AGP/Kotlin versions actually used, and
the emulator/device the instrumented evidence was collected on.

- `applicationId` org.openlife, `versionCode` 1, `versionName` 0.0.1-c0
- Gradle 9.7.1, AGP 9.4.0, Kotlin 2.4.10 (see `gradle/libs.versions.toml`)
- Device: `dev36` AVD (Pixel 7 profile, API 36 `google_apis` x86_64), GPU
  passthrough per the launch command in `CLAUDE.md`
- Host: AMD Ryzen Embedded V1756B / Radeon Vega Gfx, 4 CPU cores allotted to
  this sandbox (shared with other concurrent sessions - see Known
  environment limitations)

## Performance (design §12)

Targets: under 2s to reopen a saved 4 MiB image; under 5s to prepare it
after the provider begins delivering bytes. Both measured 2026-09-14 on
`dev36` as configured above, debug build (not release-optimized, so these
are conservative relative to a release build), using a real ~3.8 MB
2300×1720 noise JPEG (`TestHostileContentProvider`'s on-demand
`noise-<w>x<h>.jpg`, the same real AES-256-GCM/SQLCipher path every other
Stage 8 measurement used) as the closest fixture to the design's own 4 MiB
baseline. Timings taken from device-local `logcat` timestamps on both ends
of each measured span (host/device clock skew of ~2.1s was discovered and
ruled out as a measurement artifact - see the note below the table).

| Measurement | Condition | Result | Target |
| --- | --- | --- | --- |
| Prepare (share intent → `Prepared` state) | Cold: fresh process, includes Keystore + SQLCipher bootstrap and one recovery pass | 3.36s | < 5s |
| Prepare (share intent → `Prepared` state) | Warm: same process, vault already bootstrapped | 0.86s | < 5s |
| Reopen (viewer tap → decrypted bytes) | The `SourceViewRepository.loadReadyBytes` call itself, vault already bootstrapped by the list screen's own load (the realistic case - a user cannot tap a list item before the list itself has loaded, which already forces bootstrap) | 28ms | < 2s |

Both pass with considerable margin. The cold-prepare case is dominated by
one-time process/Keystore/SQLCipher startup cost (~2.5s of the 3.36s),
not by the actual bounded-read/validate/sample-decode/encrypt work on the
3.8 MB file itself (the warm case isolates that at well under 1s).

Peak memory (`adb shell dumpsys meminfo org.openlife`) immediately after
decrypting and displaying the 3.8 MB image in the viewer: 138,971 KB PSS
total (Dalvik heap 40,977 KB, native heap 25,209 KB) on this debug build.
No full-size, unsampled bitmap allocation was observed to spike this
further, consistent with the design requirement that only a sampled
decode ever reaches the screen.

**Not measured, recorded as a gap**: a true baseline/reference device (this
evidence is emulator-only, per the environment limitations below), a
16 MiB near-limit file specifically (the 3.8 MB fixture is the closer match
to the design's own "reopen a 4 MiB image" baseline; C0-06/C0-09/C0-10 use
larger ~12 MB and ~16 MiB-adjacent fixtures for validation and fault
injection, not for this specific timing claim), and a release (as opposed
to debug) build's numbers, which would very likely be materially faster
(no JIT-cold interpreter overhead, no debuggable-process tax).

## Stage 0 emulator install/launch check

`./gradlew :app:installDebug` succeeded on `dev36` (API 36); `adb shell am
start -n org.openlife/org.openlife.app.MainActivity` launched it, and
`dumpsys activity activities` confirmed `topResumedActivity` stayed
`org.openlife/.app.MainActivity` throughout. `adb logcat` showed no
`FATAL EXCEPTION` or crash referencing `org.openlife` at any point.

Note for future stages: this emulator instance was, independently of
OpenLife, under heavy host CPU contention during this session — cold boot
took over 30 minutes with the AVD's existing snapshot and repeated
`system_server` watchdog restarts, then ~50 seconds after `-wipe-data`
discarded the stale snapshot. After launching OpenLife, the *system*
Pixel Launcher and System UI (not OpenLife) intermittently showed "isn't
responding" dialogs from the same host contention, which is why a clean
screenshot of the placeholder screen wasn't captured this session. This is
recorded as an environment characteristic to expect in later stages, not a
defect in the app under test.

## Stage 6 bugs found by running the instrumented suite (not by inspection)

- `TestHostileContentProvider`'s original authority
  (`org.openlife.debug.testprovider`) was itself nested under
  `org.openlife.`, so `IntakeIntentValidator`'s own-authority check —
  working exactly as designed — rejected every accept-path test's URI as
  "pointing back into OpenLife." Every such test hung until its 30s
  timeout waiting for a status that a synchronous rejection had already
  overwritten before the test ever looked. Fixed by renaming the test
  provider's authority to `net.openlifetest.hostileprovider`, outside the
  app's own namespace, with a comment at the constant explaining why.
- `IntakeActivity.importFrom` caught `SecurityException` around
  `contentResolver.openInputStream(uri)` but not `FileNotFoundException`,
  which is exactly what a failing provider throws (and did, in
  `TestHostileContentProvider.failOpen`) — an uncaught crash instead of the
  intended "please select it again" path. Fixed by also catching
  `FileNotFoundException`.

Also found, in the test harness itself rather than app code:
`ActivityScenario.launch(intent)` with an *implicit* intent resolves
through normal Android intent resolution (which can hit the system
chooser, a different resolved component, or nothing at all for an action
`IntakeActivity` doesn't declare) — it does not, by itself, target the
activity being tested. Every `IntakeActivityTest` intent now sets an
explicit `component` naming `IntakeActivity`, which is also the more
correct test for design §8's actual concern (a caller invoking the
exported activity directly, bypassing the declared filter).

## Stage 7 findings

- Adding the first-run explanation gate (design §8) silently broke every
  existing `IntakeActivityTest` case: the gate blocks the intake flow
  entirely until acknowledged, and the tests never tapped "I understand,"
  so every one of them hung until its timeout waiting for a status the
  ViewModel could never reach. Fixed by having the test suite's `@Before`
  call `FirstRunPreferences.setAcknowledged(...)` up front, simulating a
  returning user whose device already saw onboarding - this suite tests
  intake behaviour, not onboarding, so that's the correct simulation, not
  a workaround. No dedicated automated test yet confirms the first-run
  screen itself blocks and dismisses correctly; recorded as a gap for
  Stage 8.
- `adb screencap`/`exec-out screencap` return a black image once
  `FLAG_SECURE` is applied (confirmed expected, not a bug, via `dumpsys
  window org.openlife` showing the `SECURE` flag present) - visual
  verification for secured screens in this environment goes through
  `uiautomator dump`'s text hierarchy instead.

- **Cross-test hang in `IntakeAndListFlowTest`, found while adding C0-17
  coverage.** Adding a test that simulated "provider never responds" via a
  real, unwritten OS pipe closed from another thread made an *unrelated*
  later test in the same instrumented process
  (`savingThroughIntakeViewModelMakesTheSourceAppearInTheListAndDeletionRemovesIt`)
  hang for its full timeout when run as part of the suite, though it passed
  in isolation. First hypothesis (wrong, but worth recording since it cost
  real time): closing a real fd from another thread while a read is
  blocked on it is a known-hazardous race that can corrupt the process's fd
  table, and was assumed to be leaking into later file opens. That
  mechanism was removed from `TestHostileContentProvider` entirely (a safe
  JVM-level equivalent using a plain in-memory blocking `InputStream` was
  added as `BoundedStreamReaderTest.closingTheStreamFromAnotherThreadUnblocksABlockedRead`,
  `:vault:test`) — but re-running the suite showed the *same* hang still
  present, disproving that hypothesis. Root-caused instead with a live
  thread dump (`adb shell debuggerd -b <pid>`) plus temporary logging
  through `MutationQueue`/`OpenLifeApp.vault()`/`IntakeViewModel.startImport`
  (all removed again once diagnosed): the actual cause was
  `restoringFromASavedStateHandleReAuthenticatesTheStagePreview` calling
  `restored.cancel()` as its last statement without awaiting the resulting
  `IntakeUiState.Cancelled` — `cancel()` runs fire-and-forget on
  `viewModelScope` and holds the process-wide `MutationQueue` mutex while it
  runs. The next test's own import then raced it: `MutationQueue.tryAcquire`
  correctly returned "busy," `IntakeViewModel` correctly mapped that to
  `IntakeUiState.Busy` (not a bug), and the test's own
  `awaitState { it is Preview }` then waited the full timeout for a state
  that was never coming — a test-isolation bug (a dangling coroutine
  outliving its test method on a singleton shared across every test in the
  process), not a defect in `MutationQueue`, `ImportRepository`, or
  `IntakeViewModel`. Fixed by awaiting `Cancelled` before the test method
  returns. Re-verified stable across 3 consecutive full
  `:app:connectedDebugAndroidTest` runs (9/9 passing each time) after the
  fix. Lesson carried forward: any test that triggers a `viewModelScope`
  fire-and-forget operation touching the shared `MutationQueue` must await
  its terminal state before returning, since the queue and the `OpenLifeApp`
  singleton are shared by every test class in one instrumented process.

## Known environment limitations (carried into the handoff)

- No physical Android device available; all instrumented evidence comes from
  the `dev36` emulator (API 36, Pixel 7 profile, x86_64).
- No API 29 (declared `minSdk`) system image installable — the Android SDK in
  this environment is a read-only Nix store path.
- Device-transfer and manufacturer-specific backup behaviour (R2) cannot be
  fully exercised on an emulator.
- `adb shell pm clear org.openlife` behaved like a full uninstall at least
  twice during Stage 8's manual on-device passes (`run-as org.openlife`
  afterward reported `unknown package`, and `pm list packages` no longer
  listed it) rather than the expected "wipe data, keep the app installed"
  behaviour — worked around by re-running `./gradlew :app:installDebug`
  immediately after. Not investigated further since it never affected an
  automated instrumented-test run (only ad hoc manual `adb`-driven checks),
  but worth knowing before relying on `pm clear` alone in a future manual
  session on this emulator.
- This sandbox has only 4 CPU cores and runs multiple concurrent agent
  sessions plus resident Gradle/Kotlin daemons. The emulator crashed on
  every boot attempt (~10 attempts, ~1.5 hours) until those competing
  daemons were stopped (`./gradlew --stop`, killing the Kotlin compile
  daemon) and the emulator was launched with `-cores 1 -no-metrics
  -crash-report-mode disabled` — after which it booted in under a minute
  and stayed usable. `system_server` still restarted itself at least twice
  more during the session (visible as a changed `pidof system_server`
  between checks) without ever bringing the device down; one instrumented
  run happened to race a restart and failed with `DeadSystemException`,
  and simply re-running succeeded. Future stages should expect to retry a
  `connectedAndroidTest` invocation if it fails with no test-code-shaped
  cause, before assuming the code is at fault.
- Confirmed directly (a stray logcat file from a different package,
  `net.aieat.netswissknife`, appeared in this project's own test-results
  directory): this `dev36` emulator instance is shared with at least one
  other, unrelated concurrent session on the same host.
- **GPU passthrough resolved.** `-gpu host` first failed outright
  (`Failed to get EGL display`) because the sandbox had no X server; after
  the host environment added one (`DISPLAY=:0`), `-gpu host` alone still
  crashed the emulator process itself with a **SIGSEGV** (confirmed via
  exit code 139, captured by wrapping the emulator invocation to record its
  real exit status rather than inferring from logs) — a real bug in the
  gfxstream↔RADV (AMD Mesa Vulkan) interaction on this host's AMD Ryzen
  Embedded V1756B / Vega iGPU, not a sandbox permission or memory limit
  (checked and ruled out: cgroup memory well under limit, no OOM-kill
  signal, no coredump). Passing `-feature -Vulkan` alongside `-gpu host`
  disables only the guest Vulkan path while keeping GLES host-accelerated,
  and this is stable: confirmed booting in ~33s (vs. minutes under software
  rendering) with `Graphics Adapter: ... AMD Ryzen Embedded V1756B with
  Radeon Vega Gfx (radeonsi, raven, ACO) ... Mesa 26.1.8` in the log, and
  the full `:vault` (39) and `:app` (6) instrumented suites passing under
  it. **Current working launch command:**
  `emulator -avd dev36 -no-window -no-audio -no-boot-anim -gpu host -feature -Vulkan -no-snapshot -no-metrics -crash-report-mode disabled`.
  Software rendering (`-gpu swiftshader_indirect`) remains a documented
  fallback if this host's GPU driver ever regresses again.
