# P0-10 verification: restore a green CI gate

Date: 2026-09-23. Branch: `plan/P0-10-ci-green-and-core-ux`. Local device:
`dev36` (API 36, google_apis x86_64, host GLES, 1080x2400 at 420 dpi).

## Root causes (from hosted runs 35800443855 and 35839602476)

| Job | Failure | Cause |
| --- | --- | --- |
| Build, lint, unit tests | `Validate plan` exit 2: "snakeyaml jar not found" | The runner's Gradle cache had no unpacked wrapper distribution when `plan-check.sh` ran, so it could not find SnakeYAML. Lint, JVM tests and `assembleDebug` never ran. |
| Build, lint, unit tests (latent) | lint: 2 errors | `PluralsCandidate` on `viewer_display_rotation`; `UnusedResources` on `about_title`. Earlier P1-05 and P1-06 evidence already recorded both. |
| Instrumented API 29 and 36 | `ViewerScreenC1Test` x2 | On CI's small AVD screen the OCR text sits below the 320 dp image and the details card. `assertIsDisplayed` fails without a scroll. |
| Instrumented API 29 and 36 | `BrandScreenshotInstrumentedTest` | The goldens were made on the local host-GPU emulator. Under SwiftShader, `brand-components-light.png` differs in 34,942 (API 36) and 38,901 (API 29) pixels, against a tolerance of 1,152. |

## Fixes and local evidence

- **plan-check:** `scripts/plan-check.sh` runs `gradlew --version` when SnakeYAML
  is missing.
  - Command: `GRADLE_USER_HOME=<empty scratch dir> scripts/plan-check.sh`
  - Result: the wrapper downloads, then `steps=77 errors=0`, exit 0.
- **lint:** `viewer_display_rotation` has a local `tools:ignore="PluralsCandidate"`
  because it only ever takes 90, 180 or 270. `about_title` was removed; P1-17
  reintroduces it and uses it.
  - Command: `./gradlew lint`
  - Result: BUILD SUCCESSFUL.
- **ViewerScreenC1Test:** the test now scrolls to the asserted node.
  - Before the fix, reproduced at `wm size 320x640` / `wm density 160`:
    2 of 3 failed with the same messages as CI.
  - After the fix: 3/3 at 320x640/160 dpi and 3/3 at 1080x2400/420 dpi.
- **Brand goldens:** goldens live under `golden/api<SDK>-<PRODUCT>-<dpi>dpi/`.
  - The existing PNGs moved to `api36-sdk_gphone64_x86_64-420dpi/` and still
    pass there (1/1).
  - At 160 dpi the test reports
    `AssumptionViolatedException: no committed goldens for render profile api36-sdk_gphone64_x86_64-160dpi`
    and Gradle exits 0. This is a skip, not a pass.
  - `bash scripts/ci/generate-goldens.sh <scratch>` returned `OK (1 test)` and
    pulled 8 PNGs.

## Gaps

- Hosted PR run: see PR #7.
- CI-profile goldens were generated by dispatch run 35863113781 (all jobs
  green) and committed under `api29-sdk_gphone_x86_64-160dpi` and
  `api36-sdk_slim_x86_64-160dpi`. I reviewed them visually; they contain only
  synthetic content.
- API 29 has not been run locally, since no API 29 image is available in this
  sandbox.
