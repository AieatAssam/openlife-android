package org.openlife.app.intake

/** P1-02 stub: one-shot proof that an intake intent came from OpenLife's own picker. */
class PickerNonce {
    fun issue(): String = ""

    @Suppress("UnusedParameter", "FunctionOnlyReturningConstant")
    fun consume(candidate: String?): Boolean = false
}
