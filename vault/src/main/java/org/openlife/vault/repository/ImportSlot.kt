package org.openlife.vault.repository

import java.util.UUID

/** P1-15 stub: the one-import slot. [release] does nothing yet. */
class ImportSlot {
    @Suppress("UnusedParameter")
    fun release(sourceId: UUID) = Unit
}
